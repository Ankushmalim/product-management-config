server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_user_db?createDatabaseIfNotExist=true
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"